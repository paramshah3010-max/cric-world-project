# Optional: base URL for the CRIC WORLD API.
# Leave empty in development — the Vite dev server proxies /api to the backend
# (see vite.config.ts). Set this only when the API is on a different origin.
VITE_API_BASE_URL=
